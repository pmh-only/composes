server {
	listen 80;

	server_name backup.mc.shutupandtakemy.codes;

	root /var/backups;

	location / {
		autoindex on;
		autoindex_format json;
	}

	location ~* /(.+\.tar\.gz)$ {
    add_header Content-disposition "attachment; filename=$1";
	}
}
